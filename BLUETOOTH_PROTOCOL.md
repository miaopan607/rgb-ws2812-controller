# 蓝牙 App 控制协议

本文档面向手机 App 开发者，说明通过蓝牙串口控制 FPGA 彩灯板的二进制协议。

## 串口参数

- 波特率：9600
- 数据位：8
- 校验位：无
- 停止位：1
- 发送格式：二进制字节流，不是 ASCII 十六进制字符串

## 可变长度帧格式

每条命令长度为 `11~18` 字节。板子只在完整帧校验通过后更新参数，校验失败会丢弃整帧并保持当前显示。

| 字节序号 | 字段 | 说明 |
| --- | --- | --- |
| 0 | `0xAA` | 帧头 1 |
| 1 | `0x55` | 帧头 2 |
| 2 | `mode` | 模式：`0=静态`，`1=流水`，`2=呼吸`，`3=Disco`，`4=渐变`，`5=流动渐变` |
| 3 | `R` | 红色通道，`0~255` |
| 4 | `G` | 绿色通道，`0~255` |
| 5 | `B` | 蓝色通道，`0~255` |
| 6 | `brightness` | 全局亮度，`0=灭`，`255=最大` |
| 7 | `period` | 呼吸模式单位 `100ms`，渐变/流动渐变单位 `50ms`，发送 `0` 时板子按 `1` 处理 |
| 8 | `flow_count` | 流水画面数量，范围 `1~8` |
| 9~`8+flow_count` | `frame0..frameN-1` | 每个画面 1 字节灯掩码，bit0~bit7 对应 LED0~LED7 |
| 最后 1 字节 | `checksum` | 从 `mode` 到最后一个 `frame` 逐字节 XOR |

## 字段生效范围

- 静态模式：使用 `R/G/B/brightness`，8 颗灯同色常亮。
- 流水模式：使用 `R/G/B/brightness/flow_count/frame0..frameN-1`，每 250ms 切换到下一个画面。
- 呼吸模式：使用 `R/G/B/brightness/period`，亮度在 `0~brightness` 之间按三角波变化。
- Disco 模式：使用 `brightness`，颜色使用板子原有的离散三色循环。
- 渐变模式：使用 `brightness/period`，8 颗灯同色做平滑 RGB 环渐变。
- 流动渐变模式：使用 `brightness/period`，8 颗灯沿同一 RGB 环均匀错相并整体流动。

非流水模式仍建议带上默认 `flow_count` 和画面数据，保证 App 始终发送同一种帧结构。

## 流水画面掩码

每个 `frame` 是一个 8 bit 掩码：

| bit | LED |
| --- | --- |
| 0 | LED0 |
| 1 | LED1 |
| 2 | LED2 |
| 3 | LED3 |
| 4 | LED4 |
| 5 | LED5 |
| 6 | LED6 |
| 7 | LED7 |

例如：

- `0x01`：只亮 LED0。
- `0x03`：同时亮 LED0 和 LED1。
- `0xFF`：8 颗灯全亮。
- `0x00`：该画面全灭。

基础流水仍然保留在 App 中，但底层会转换成新版协议。例如基础顺序 `0,1,2,3,4,5,6,7` 会转换成 8 个画面：

```text
01 02 04 08 10 20 40 80
```

## 校验算法

`checksum = mode ^ R ^ G ^ B ^ brightness ^ period ^ flow_count ^ frame0 ^ ... ^ frameN-1`

示例伪代码：

```c
uint8_t checksum = 0;
for (int i = 2; i <= 8 + flow_count; i++) {
    checksum ^= frame[i];
}
frame[9 + flow_count] = checksum;
```

## 示例帧

静态红色，亮度 128，附带默认 8 个流水画面：

```text
AA 55 00 FF 00 00 80 14 08 08 04 02 01 10 20 40 80 9C
```

流水绿色，亮度 96，基础顺序 `0,1,2,3,4,5,6,7`：

```text
AA 55 01 00 FF 00 60 14 08 01 02 04 08 10 20 40 80 7D
```

高级流水绿色，亮度 96，4 个画面分别为 `03,0C,30,C0`：

```text
AA 55 01 00 FF 00 60 14 04 03 0C 30 C0 71
```

呼吸蓝色，亮度 128，完整周期 2 秒：

```text
AA 55 02 00 00 FF 80 14 08 08 04 02 01 10 20 40 80 9E
```

Disco 模式，亮度 64：

```text
AA 55 03 00 00 00 40 14 08 08 04 02 01 10 20 40 80 A0
```

渐变模式，亮度 64，完整一轮 1 秒：

```text
AA 55 04 00 00 00 40 14 08 08 04 02 01 10 20 40 80 A7
```

流动渐变模式，亮度 64，完整一轮 1 秒：

```text
AA 55 05 00 00 00 40 14 08 08 04 02 01 10 20 40 80 A6
```

## App 端注意事项

- 每次发送建议发送完整二进制帧，不要拆成多个逻辑命令。
- `flow_count` 必须为 `1~8`，否则板子会丢弃整帧。
- `frame` 允许为 `0x00`，表示该流水画面全灭。
- 如果用户只调某个参数，App 也应带上当前模式的其他参数重新发送完整帧。
- 亮度为全局缩放，RGB 通道仍可设置为任意 `0~255`。
