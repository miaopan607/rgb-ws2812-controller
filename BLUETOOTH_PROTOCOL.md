# 蓝牙 App 控制协议

本文档面向手机 App 开发者，说明通过蓝牙串口控制 FPGA 彩灯板的二进制协议。

## 串口参数

- 波特率：9600
- 数据位：8
- 校验位：无
- 停止位：1
- 发送格式：二进制字节流，不是 ASCII 十六进制字符串

## 固定帧格式

每条命令固定 17 字节。板子只在完整帧校验通过后更新参数，校验失败会丢弃整帧并保持当前显示。

| 字节序号 | 字段 | 说明 |
| --- | --- | --- |
| 0 | `0xAA` | 帧头 1 |
| 1 | `0x55` | 帧头 2 |
| 2 | `mode` | 模式：`0=静态`，`1=流水`，`2=呼吸`，`3=渐变` |
| 3 | `R` | 红色通道，`0~255` |
| 4 | `G` | 绿色通道，`0~255` |
| 5 | `B` | 蓝色通道，`0~255` |
| 6 | `brightness` | 全局亮度，`0=灭`，`255=最大` |
| 7 | `period` | 呼吸完整周期，单位 `100ms`，发送 `0` 时板子按 `1` 处理 |
| 8~15 | `order0..order7` | 流水灯序，必须是 `0~7` 的不重复排列 |
| 16 | `checksum` | 从 `mode` 到 `order7` 共 14 字节逐字节 XOR |

## 字段生效范围

- 静态模式：使用 `R/G/B/brightness`，8 颗灯同色常亮。
- 流水模式：使用 `R/G/B/brightness/order0..order7`，单颗灯按自定义顺序移动。
- 呼吸模式：使用 `R/G/B/brightness/period`，亮度在 `0~brightness` 之间按三角波变化。
- 渐变模式：暂时只使用 `brightness`，颜色使用板子内置的红绿蓝渐变。

## 校验算法

`checksum = mode ^ R ^ G ^ B ^ brightness ^ period ^ order0 ^ ... ^ order7`

示例伪代码：

```c
uint8_t checksum = 0;
for (int i = 2; i <= 15; i++) {
    checksum ^= frame[i];
}
frame[16] = checksum;
```

## 示例帧

静态红色，亮度 128：

```text
AA 55 00 FF 00 00 80 14 00 01 02 03 04 05 06 07 6B
```

流水绿色，亮度 96，顺序 `0,1,2,3,4,5,6,7`：

```text
AA 55 01 00 FF 00 60 14 00 01 02 03 04 05 06 07 8A
```

呼吸蓝色，亮度 128，完整周期 2 秒：

```text
AA 55 02 00 00 FF 80 14 00 01 02 03 04 05 06 07 69
```

渐变模式，亮度 64：

```text
AA 55 03 00 00 00 40 14 00 01 02 03 04 05 06 07 57
```

## App 端注意事项

- 每次发送建议发送完整 17 字节帧，不要拆成多个逻辑命令。
- 流水灯序必须包含 `0,1,2,3,4,5,6,7` 且不能重复，否则板子会丢弃整帧。
- 如果用户只调某个参数，App 也应带上当前模式的其他参数重新发送完整帧。
- 亮度为全局缩放，RGB 通道仍可设置为任意 0~255。
